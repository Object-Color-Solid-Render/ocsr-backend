# ocsr-backend

## Setup

### First-time Setup
```bash
python3 -m venv ocsr_venv
source ocsr_venv/bin/activate

pip install -r requirements.txt
```

### Setup (re-run every time)

```bash
source ocsr_venv/bin/activate
```

## Run

```bash
python3 app.py
```
